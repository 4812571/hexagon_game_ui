struct TrailsData {
    EquippedId: string?,
    OwnedIds: map {[string]: boolean},
}

function PurchaseTrail {
    Yield: Coroutine,
    Data: string,
}

function EquipTrail {
    Yield: Coroutine,
    Data: string?,
}