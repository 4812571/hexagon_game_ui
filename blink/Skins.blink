struct SkinsData {
    OwnedSkins: map {[string]: boolean},
    ActiveSkinId: string?,
}

function PurchaseSkin {
    Yield: Coroutine,
    Data: string,
}

function EquipSkin {
    Yield: Coroutine,
    Data: string?,
}